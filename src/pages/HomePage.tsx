import { Contact, FeaturedProducts, Hero, Services } from '../components'

function HomePage() {
    return (
        <main>
            <Hero />
            <FeaturedProducts />
            <Services />
            <Contact />
        </main>
    )
}

export default HomePage
