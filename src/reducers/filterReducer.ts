import { TFilterAction, TFilterState } from '../types/filterTypes'

function filterReducer(
    state: TFilterState,
    action: TFilterAction,
): TFilterState {
    return state
}

export default filterReducer
